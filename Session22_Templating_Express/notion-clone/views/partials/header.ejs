<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>
    <%= typeof title !=='undefined' ? title + ' - NoteMaster' : 'NoteMaster - Your All-in-One Workspace' %>
  </title>
  <link rel="stylesheet" href="/css/style.css" />
</head>

<body>
  <header class="header">
    <div class="header-container">
      <div class="header-left">
        <a href="/" class="logo">
          <span class="logo-icon">📝</span>
          <span class="logo-text">NoteMaster</span>
        </a>
      </div>

      <nav class="header-nav">
        <% if (user) { %>
          <a href="/workspaces" class="nav-link">
            <span>🏢</span> Workspaces
          </a>
          <a href="/templates" class="nav-link"> <span>📄</span> Templates </a>
          <div class="user-menu">
            <button class="user-button">
              <span class="user-avatar">
                <%= user.avatar %>
              </span>
              <span class="user-name">
                <%= user.fullName %>
              </span>
              <span class="dropdown-arrow">▼</span>
            </button>
            <div class="user-dropdown" id="userDropdown">
              <div class="dropdown-header">
                <div class="dropdown-user-info">
                  <span class="dropdown-avatar">
                    <%= user.avatar %>
                  </span>
                  <div>
                    <div class="dropdown-name">
                      <%= user.fullName %>
                    </div>
                    <div class="dropdown-email">
                      <%= user.email %>
                    </div>
                  </div>
                </div>
              </div>
              <div class="dropdown-divider"></div>
              <a href="/logout" class="dropdown-item">
                <span>🚪</span> Log Out
              </a>
            </div>
          </div>
          <% } else { %>
            <a href="/login" class="nav-link">Log In</a>
            <a href="/register" class="btn btn-primary">Get Started</a>
            <% } %>
      </nav>
    </div>
  </header>

  <main class="main-content">