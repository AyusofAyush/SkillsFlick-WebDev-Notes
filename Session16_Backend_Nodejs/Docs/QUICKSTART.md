# 🚀 Quick Start Guide

## Get Started in 3 Steps

### 1️⃣ Install Dependencies

```bash
# Backend
cd backend
npm install

# Frontend  
cd ../frontend
npm install
```

### 2️⃣ Start Backend Server

```bash
cd backend
npm start
```

You should see:

```
🚀 ================================
   Session 16 Backend Server
   ================================
   ✅ Server running at http://localhost:3001
```

### 3️⃣ Start Frontend Application

**Open a new terminal window** and run:

```bash
cd frontend
npm start
```

The application will automatically open at `http://localhost:3000`

## ✅ Success

You should now see:

- **Backend Server**: Running on port 3001
- **Frontend App**: Running on port 3000
- **Server Status**: Shows "✅ Online" in the header

## 🎯 What to Try

1. **Client-Server Demo**: Click "Send Request to Server" to see the request-response cycle
2. **HTTP Methods**: Create, read, update, and delete users
3. **Server Info**: View system information
4. **File Operations**: Create and read log files

## 🛑 To Stop

Press `Ctrl+C` in both terminal windows

## 📖 Need More Help?

See `PROJECT_README.md` for detailed documentation.

---

**Happy Learning! 🎓**
